#HELP -> 返回帮助面板，大小写不区分
#PRICE 601988 -> 实时查询股票代码 601988 的数据
#KDJ 601998 -> 查询股票代码 601998 的数据
#NEWS 601998 -> 返回 7 日内 601998 的新闻数据，优先用 Markdown 展示站内资讯 + Tavily 全网搜索；若开启 #GRAPH on 且存在可用图片链接，则附带图片
#BUY 601998 100 7.2 -> 以 7.2 元买入 100 股 601998（需要配置交易服务商）
#SELL 601998 100 7.25 -> 以 7.2 元卖出 100 股 601998
#MA10 601998 -> 查询 601998 的 10 日均线
#CONFIG 浙商证券 -> 开始浙商证券登录流程配置
#CONFIG 查看 -> 查看当前券商配置和流程状态
    - 全登录流程: 用户触发登录
    - bot 要求用户券商信息
    - 用户发送券商账号
    - bot 要求用户发送密码（b64 encode）
    - 用户发送 b64 的密码
    - bot 完成登录 存储信息
    - 60s 内不回复自动退出

#CANCEL -> 取消当前进行中的多轮流程
#q -> 退出当前进行中的多轮流程

#ANALYZE 601998 -> 先抓取行情/估值/财报/站内资讯/Tavily 检索结果，再交给 AI 做专业分析；返回 Markdown 长文，不要只给一句“偏积极/偏谨慎”
#ANALYZE_MCP [MCP_TOOL tavily_search,db_search] 601998 -> 优先利用 Tavily / 数据库等 MCP 工具补料后再做 AI 分析，输出 Markdown 专业报告
#T1 601998 -> 返回 601998 1 日内的数据
#T7 601998 -> 返回 601998 7 日内的数据
#T30 601998 -> 返回 601998 30 日内的数据
#T365 601998 -> 返回 601998 365 日内的数据

#GRAPH on/off -> 开启/关闭图文模式；on 时优先返回 Markdown 图文结果，若存在可用图片链接则附图；off 时返回纯文本 / 纯 Markdown
(后续可继续扩展为网页渲染 + 截图返回，目前至少要保证 Markdown 和可用图片链接能正常渲染)

#指数 上证指数 -> 返回上证指数信息

#新股 -> 直接返回当日可申购的新股信息 如果没有则返回今日无可申购的新股.

#SEARCH 601998 -> 返回 601998 的 Tavily 全网搜索结果，优先 Markdown 展示标题、摘要、来源和链接

网页端 AI 多角色分析:
- AI 多角色分析 601998
- 一键辩论
- 多头
- 空头
- 裁判

AI 错误提示:
- 如果出现 “AI 接口调用失败，请检查 API 地址和密钥”，说明 .env 中 AI API 地址 / 密钥 / 模型尚未配置完成，或当前配置不可用
- #ANALYZE 在 AI 失败时也应先把已抓取材料整理后返回，不要只报错
- 回复内容优先 Markdown，不要堆原始指标；先解释含义，再给结论

//注: 601998 等代号可以替换为文字, AAPL/BABA/中信银行/中信证券/中国银行等等内容
//全部都尽可能详细 如果无数据返回 NULL.
